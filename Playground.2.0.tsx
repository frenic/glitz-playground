import * as React from 'react';
import { styled } from '@glitz/react';

const Text = styled.span({ color: 'thistle' });

const Playground = () => <styled.Span css={{ color: 'coral' }}>Hello world!</styled.Span>;

export default Playground;
